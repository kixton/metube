
<div class="main">
<h2>All Playlists</h2>

<%= link_to("New playlist", {action: "new"}, class: "button tiny") %><br>

<% @playlists.each do |playlist| %>

  <%= link_to(playlist.listname, playlist_path(playlist)) %> by
  <em><%= playlist.user.name %></em>

  <br>

<% end %>

</div>


